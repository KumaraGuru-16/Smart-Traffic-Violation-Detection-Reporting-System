{
  "name": "traffic-violation-system",
  "version": "1.0.0",
  "scripts": {
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm start",
    "dev": "concurrently --names backend,frontend --prefix-colors blue,green \"cd backend && npm start\" \"cd frontend && npm start\""
  },
  "dependencies": {
    "axios": "^1.15.2",
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "pptxgenjs": "^4.0.1",
    "react-router-dom": "^7.14.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
